[project]
name = "consumer-gpu-p2p-vllm-guide"
version = "2.0.0"
description = "Integrity-gated NVIDIA consumer GPU P2P setup for vLLM"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
install-p2p = "install:main"
